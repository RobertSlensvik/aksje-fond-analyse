[pytest]
testpaths = tests
# Uten dette må testene kjøres som `python -m pytest` fra prosjektroten for at
# `import analyse` skal treffe. Med pythonpath fungerer bare `pytest` også.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*OpenSSL.*:Warning
